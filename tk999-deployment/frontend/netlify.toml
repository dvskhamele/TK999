# Netlify configuration file
[build]
  command = "npm run build"
  publish = "dist"
  base = "tk999-deployment/frontend"
  environment = { NODE_VERSION = "20" }

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200